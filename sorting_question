class Solution(object):
    def findNonMinOrMax(self, nums):
        if(len(nums)>1):
            nums.remove(max(nums))
            nums.remove(min(nums))
            if(nums!=[]):
                return nums[0]
            else:
                return -1    
        else:    
            return -1
        
